<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================== -->
    <!-- 亮色主题：白底 + 黑字 + 深蓝表头                  -->
    <!-- ============================================== -->

    <!-- 颜色（Color 类型） -->
    <Color x:Key="BackgroundColor">#FFFFFF</Color>
    <Color x:Key="PanelColor">#F4F5F7</Color>
    <Color x:Key="PanelLightColor">#FAFBFC</Color>
    <Color x:Key="BorderColor">#E5E7EB</Color>
    <Color x:Key="HoverColor">#EDF1F7</Color>
    <Color x:Key="SelectedColor">#D6E4F5</Color>
    <Color x:Key="AccentColor">#0C50A5</Color>
    <Color x:Key="AccentHoverColor">#0A4489</Color>
    <Color x:Key="TextColor">#1A1A1A</Color>
    <Color x:Key="MutedTextColor">#6B7280</Color>
    <Color x:Key="HeaderTextColor">#0C50A5</Color>
    <Color x:Key="SuccessColor">#15803D</Color>
    <Color x:Key="WarningColor">#B45309</Color>
    <Color x:Key="ErrorColor">#DC2626</Color>

    <!-- 画刷（Brush 类型，Background/Foreground 等属性引用这种） -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
    <SolidColorBrush x:Key="PanelLightBrush" Color="{StaticResource PanelLightColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>
    <SolidColorBrush x:Key="SelectedBrush" Color="{StaticResource SelectedColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}"/>
    <SolidColorBrush x:Key="HeaderTextBrush" Color="{StaticResource HeaderTextColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

    <!-- 字体 -->
    <FontFamily x:Key="UiFont">Segoe UI, Microsoft YaHei UI, PingFang SC, sans-serif</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Code, Consolas, Courier New, monospace</FontFamily>

</ResourceDictionary>
